---
name: Bug report
about: confdiff reported the wrong thing, crashed, or missed a real change
title: ''
labels: bug
assignees: ''
---

**What happened**
A clear description of the incorrect behavior.

**Inputs to reproduce**
The two files (or a minimal reduction). Paste them inline in fenced code blocks:

File A (`a.yaml`):
```yaml
```

File B (`b.yaml`):
```yaml
```

**Command**
```
confdiff a.yaml b.yaml ...
```

**Expected output**
What confdiff should have printed.

**Actual output**
What confdiff actually printed.

**Environment**
- confdiff version (`confdiff --version`):
- Node version (`node --version`):
- OS:
